import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
          main: '#808080',
        },
        secondary: {
          main: '#E5E5E5',
        },
    },
});

export default theme;
